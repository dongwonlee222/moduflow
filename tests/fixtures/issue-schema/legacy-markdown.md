# Issue: `legacy-markdown` Legacy Markdown contract

**Status: backlog** — created 2026-07-22.
**Priority: p1**
**Blocked-by: `001-dependency`**

## Context

This fixture exercises the legacy Markdown parser without frontmatter.
