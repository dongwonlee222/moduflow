# MSW Fixture Catalog

Issue: `<issue-id>`
Spec: `<spec path>`
Owner:
Reviewer:
Status: draft

## Fixture Catalog

| Fixture | Contract / Endpoint | Scenario | Response Shape | Used By |
| --- | --- | --- | --- | --- |
| Example: `itemsSuccess` | `GET /api/items` | populated success | item list fields | Storybook, smoke |
| Example: `itemsEmpty` | `GET /api/items` | empty result | empty list | Storybook |
| Example: `itemsError` | `GET /api/items` | server error | error payload | Storybook, smoke |

## Coverage

- Success states:
- Empty states:
- Error states:
- Permission/role states:
- Slow/loading states:

## Review Notes

- Fixtures reused from existing project:
- Fixtures intentionally not mocked:
