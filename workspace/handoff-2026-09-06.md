# Handoff — 2026-09-06

새 세션은 **이 문서를 먼저 읽고** 시작한다.

## 1. ⚠️ 다른 세션이 같은 저장소에서 동시에 작업 중이다

이 문서를 쓰는 도중(2026-09-06) **다른 세션이 커밋 4건을 넣었다.** 원래 여기 적으려던
"미커밋 변경 유실 경고"는 그 세션이 전부 커밋해서 해소됐다.

```
575ce60 docs(131): file the command surface as its own issue
1b266a2 docs(inbox): revise the command surface report to bilingual
3c50b89 docs(inbox): carry the Korean-first command surface report
2bdf195 docs(126,127): close both, and stop quoting code that is already fixed
```

`2bdf195`에 Beads 실측 벤치마크가 포함돼 안전하게 저장됐다. 126·127은 close됐고,
새 이슈 131(command surface가 너무 넓고 영어 전용)이 생겼다.

**이것 자체가 이번 세션이 문서화한 동시 작업 문제의 실사례다.** 두 세션이 같은
체크아웃에서 움직였고, `.moduflow/state.json`은 전역 파일 하나다. 아래 §5의 native
수정이 왜 필요한지에 대한 근거로 이 사건을 인용할 수 있다.

현재 로컬 변경(이 세션이 만든 것):

```
 M .moduflow/state.json          (next_command → 이 문서)
 M workspace/inbox.md            (2026-09-06 지식 레이어 항목 추가)
?? memory/decisions/2026-09-06-beads-issue-backend-not-adopted.md
?? knowledge/benchmarks/2026-09-06-issue-and-knowledge-tooling-landscape.md
?? workspace/handoff-2026-09-06.md
```

새 세션은 시작 전에 `git status`와 `git log --oneline -5`를 **직접 다시 확인할 것.**
이 목록도 이미 낡았을 수 있다.

## 2. 이번 세션의 결론

**Beads를 지금 채택하지 않는다.** 근거 5가지와 뒤집는 조건 2가지는
`memory/decisions/2026-09-06-beads-issue-backend-not-adopted.md`에 있다.
요약하면 — 실측이 통과시킨 것은 한 체크아웃 안의 두 프로세스인데, 실제로 필요한
것은 두 컴퓨터이고 그 구간은 미검증이다.

확정된 사실 두 가지:

- `team-state.json`의 lock은 **강제되지 않는다**. `project_workflow.py:194
  upsert_team_item`이 기존 `lock_state`/`locked_by`를 읽지 않고 무조건 병합하고,
  `start_issue_work:242-243`이 그대로 덮어쓴다. 코드 확인 완료.
- **걷어낼 execution runtime은 존재하지 않는다.** worktree를 생성하는 코드는 0건이고
  `worker_orchestrator.py:408`·`476`의 문자열 두 곳뿐이다. Issue 112의 Stream C·E는
  실행 코드 삭제가 아니라 **host 어휘 이동**이다.

## 3. 지금 할 것 — 순서대로

1. **이 세션 산출물 커밋** — 결정 기록 1건, 지형 조사 1건, 이 문서, inbox 항목,
   `state.json`. 다른 세션과 겹치지 않는지 `git status`를 먼저 볼 것.
2. **문서 사실 오류 2건 정정**
   - `issues/112-...md:117-120` — "§15 네 결정 대기 중"은 **사실이 아니다**.
     네 건 모두 2026-09-06 승인 완료(`spec.md:344`, `spec.md:350`,
     `memory/decisions/2026-09-06-112.md`, `-112-2.md`). 실제 블로커는
     **Stream C 인터페이스 리뷰**다.
   - `spec.md:365` — "승인 후 `product:plan`"은 모순이다. plan.md·tasks.md가 이미
     있고 `tasks.md:49`·`plan.md:5`는 `product:workers`라고 한다.
   - 한글본 `spec.ko.md`도 함께 고칠 것. 안 그러면 두 본이 갈라진다.
3. **Issue 112 Stream C 인터페이스 리뷰 재개** — 원래 블로커. 위 두 개가 끝나면 이어짐.

## 4. 사장님 답 대기 중 — 두 건 모두 기본값으로 이미 커밋됐다

아래 두 가지는 사장님 답을 기다리던 항목인데, 다른 세션이 **둘 다 기본값 쪽으로
커밋**했다. 되돌릴지 그대로 둘지는 여전히 사람 결정이다.

1. **`issues/112-...md:76`의 한 줄 — 커밋됨(`2bdf195`).** Beads 벤치마크가 112의
   완료된 workflow task로 연결된 상태다. 이러면 112 범위에 백엔드 결정이 묶인다.
   **권고: 되돌린다.** 112는 실행 계획 게이트 이슈이고 백엔드는 다른 축이며,
   벤치마크 자신도 "좁게 범위를 잡은 후속 이슈"를 권한다(221행).
   백엔드 결정은 `memory/decisions/2026-09-06-beads-issue-backend-not-adopted.md`에
   독립적으로 남아 있으므로, 이 줄을 빼도 근거는 사라지지 않는다.
2. **`workspace/transactions/txn-*.json` — 커밋됨.** 현재 32개가 추적된다.
   `.gitignore`에는 여전히 `.moduflow/state/`·`.moduflow/logs/`·`.playwright-mcp/`만
   있고 `workspace/transactions/`는 없다. 트랜잭션 저널은 성격상 로그에 가까워서
   저장소에 계속 쌓으면 이슈 파일 사이에 섞인다. **판단 필요**: 계속 추적할지,
   `.gitignore`에 넣고 이미 들어간 것을 정리할지.

## 5. 지금 하지 않는 것

Beads 도입, 이슈 마이그레이션, `team-state` lock 수정, P0 시험.
lock 수정과 P0는 사장님 결정 대기다.

- **native 수정 2건**(결정하면 별도 이슈 채번): `upsert_team_item`에
  compare-and-set 전제조건 추가, `team-state.json`을 직접 write에서 projection으로
  강등. 잠금 자체는 `project_lifecycle_transaction.py:2199 _acquire_lifecycle_lock`
  (`os.O_EXCL`)에 이미 있고, 키를 transaction_id → issue_id로 넓히면 된다.
- **P0 시험**: 두 컴퓨터 Dolt remote push/pull 동시 편집 + 원격 장애·복구.
  ModuFlow 코드 0줄이므로 여유 될 때 재두고 결과를 벤치마크에 덧붙인다.
  두 번째 맥이 필요하다 — 회사 Codex 환경을 쓸지 미정.

## 6. ModuFlow 밖의 미결 사항

**POKit2 처리 결정.** `~/pokit2-work`(390 이슈, 670 커밋)는 **2026-07-25 이후 커밋이
없고** npm에 `pokit2`로 배포된 상태로 살아 있다. ModuFlow와 같은 문제를 푼다.
아카이브할지, 배포를 내릴지, 합칠지 정해지지 않았다. ModuFlow repo 밖이므로
인박스에 넣지 않고 여기에 남긴다.

## 링크

- 결정: `memory/decisions/2026-09-06-beads-issue-backend-not-adopted.md`
- 실측: `knowledge/benchmarks/2026-09-06-beads-v1-2-2-empirical-issue-engine-benchmark.md`
- 지형 조사: `knowledge/benchmarks/2026-09-06-issue-and-knowledge-tooling-landscape.md`
- 신규 결함: `workspace/inbox.md` 2026-09-06 항목 (지식 레이어 미작동)
- 진행 이슈: `issues/112-execution-planner-and-backend-boundary.md`
