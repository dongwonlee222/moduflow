# ModuFlow Hub Command And 0.2.4 Release Status

## Phase

Done.

## Completed

- `/moduflow` hub command added (`commands/moduflow.md`).
- Version bumped to `0.2.4`, `release_check.py` passed.
- `main` and tag `v0.2.4` pushed.
- Codex personal marketplace registered (`INSTALLED_BY_DEFAULT`).
- README documents the `/moduflow` entry point.

## In Progress

- None.

## Blockers

- None.

## Follow-Ups

- Codex manifest version stays `0.2.3+codex.<ts>`; fix `register_codex_personal_marketplace.py` to read the base version from `.claude-plugin/plugin.json`.
- Verify `/moduflow` in a fresh Claude session and `@ModuFlow` in a new Codex thread.

## Next Command

`product:status`
